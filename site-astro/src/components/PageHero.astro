---
interface Props {
	eyebrow: string;
	title: string;
	copy: string;
}

const { eyebrow, title, copy } = Astro.props;
---

<section class="page-hero">
	<div class="eyebrow">{eyebrow}</div>
	<h1>{title}</h1>
	<p class="lede lede--narrow">{copy}</p>
</section>
